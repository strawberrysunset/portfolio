export const hex2rgb = (hex) => {
    rgb = [255, 255, 255]

    hex = #ffffff

    2^8 = 256
    16 ^ 2 = 256


    // strip hash
    // for each letter convert letter pair to rgb value
    // need a map for hex to 0 to 16
    16 * 15 -> 255

}